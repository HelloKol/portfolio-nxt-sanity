// import zod from "zod";

// const envSchema = zod.object({
//   NEXT_PUBLIC_BASE_URL: zod.string().min(1),
//   NEXT_PUBLIC_SANITY_STUDIO_ID: zod.string().min(1),
//   NEXT_PUBLIC_SANITY_STUDIO_DATASET: zod.string().min(1),
//   NEXT_PUBLIC_SENDGRID_API_KEY: zod.string().min(1),
//   NEXT_PUBLIC_GOOGLE_MAP_API_KEY: zod.string().min(1),
// });

// export const env = envSchema.parse(process.env);
